export const metadata = {
  title: 'Resume - Dean (정송헌) | iOS Developer',
  description: 'Resume of Dean Jeong, iOS Developer with Web Frontend Background',
};

export default function ResumeLayout({ children }) {
  return children;
}
